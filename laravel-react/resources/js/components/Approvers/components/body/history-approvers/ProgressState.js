import React from "react";

const ProgressState = () => {
    return (
        <div>
            <i className="fas fa-tasks"></i> Progress State.
        </div>
    );
};

export default ProgressState;
