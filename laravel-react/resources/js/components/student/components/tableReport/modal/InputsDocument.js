import React from "react";
import { Container } from "react-bootstrap";

const InputsDocument = ({ inputs }) => {
    console.log(inputs);
    return <Container className="py-2"></Container>;
};

export default InputsDocument;
